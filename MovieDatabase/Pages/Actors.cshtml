@page "{id:int?}"
@model ActorsModel
@{
    ViewData["Title"] = "Actors";
}

<table class="table">
    <thead>
        <tr>
            <th>
                First Name
            </th>
            <th>
                Last Name
            </th>
            <th></th>
        </tr>
    </thead>
    <div>
        <form asp-page="AddActor" asp-page-handler="AddActor" asp-route-id="@Model.MovieId" method="get">
            <button class="btn btn-sm btn-success">
                Add Actor
            </button>
        </form>
    </div>
    <tbody>
        @foreach (var item in Model.Actors)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    <form asp-page="ActorUpdate" asp-page-handler="update" asp-route-id="@item.id" method="get">
                        <button type="submit" class="btn btn-sm btn-primary">
                            Update
                        </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
<div>
    <a style="width: 200px; margin: 0 40%;" asp-page="Index">Back to List</a>
</div>